const Fonts = {
  display: "sans-serif-condensed",
  body: "sans-serif",
  mono: "monospace",
};

const Radii = {
  sm: 8,
  md: 14,
  lg: 20,
  xl: 28,
};

const Shadow = {
  sm: {
    shadowColor: "#000000",
    shadowOpacity: 0.08,
    shadowRadius: 6,
    shadowOffset: { width: 0, height: 2 },
    elevation: 2,
  },
  md: {
    shadowColor: "#000000",
    shadowOpacity: 0.12,
    shadowRadius: 12,
    shadowOffset: { width: 0, height: 4 },
    elevation: 4,
  },
};

export { Fonts, Radii, Shadow };
